import { DatesCountPipe } from './dates-count.pipe';

describe('DatesCountPipe', () => {
  it('create an instance', () => {
    const pipe = new DatesCountPipe();
    expect(pipe).toBeTruthy();
  });
});
