import { QuoteLikesDirective } from './quote-likes.directive';

describe('QuoteLikesDirective', () => {
  it('should create an instance', () => {
    const directive = new QuoteLikesDirective();
    expect(directive).toBeTruthy();
  });
});
